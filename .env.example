# Neon Postgres connection string (Neon dashboard -> Connection Details ->
# "Pooled connection"). Used by Prisma (lib/db.ts) for storing strategy
# signals and backtest run results.
DATABASE_URL="postgresql://user:password@ep-xxxx.neon.tech/polymarket?sslmode=require"
