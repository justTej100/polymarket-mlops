{
  "name": "polymarket-strategies",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "worker": "tsx lib/worker/liveLoop.ts",
    "prisma:generate": "DATABASE_URL=file:./dev.db prisma generate",
    "prisma:generate:neon": "NEON_DATABASE_URL=${NEON_DATABASE_URL:-postgresql://user:password@localhost:5432/polymarket} prisma generate --schema prisma/neon/schema.prisma",
    "prisma:migrate": "DATABASE_URL=file:./dev.db prisma migrate dev",
    "prisma:migrate:neon": "prisma migrate dev --schema prisma/neon/schema.prisma",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "lint": "npm run typecheck"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/ws": "^8.5.12",
    "prisma": "^5.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
